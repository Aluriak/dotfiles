#!/bin/zsh
# background: never save before shutdown modifications
# (because a directory have all files exlicited, so modify images in directory don't change feh configuration)
eval $(cat ~/.fehbg_unchanged)
